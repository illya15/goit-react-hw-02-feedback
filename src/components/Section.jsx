import { FeedbackOptions } from './FeedbackOptions';
const Section = ({title,FeedbackOptions}) => {

<button type="button" key={item} onClick={onLeavFeedback}>
  {item}
</button>; 
}
export default Section